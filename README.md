# Ramdon User API Extract.

- Program that extracts data from the ramdon user API every 2 minutes and loads it into a SQLite database, this process is automatic with Apache airflow in a python environment.